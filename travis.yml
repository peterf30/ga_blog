language: node_js
node_js:
  - 14.2.0
cache: yarn  

install: 
  - yarn test
  - yarn install

script:
  - yarn run build
  
deploy:
  provider: pages
  skip-cleanup: true
  github token: $GITHUB_TOKEN
  on:
    branch: master