language: node_js
node_js:
  - 14.2.0
cache: yarn  

install: 
  - yarn install

script:
  - yarn run test
  - yarn run build
  
deploy:
  provider: pages
  cleanup: true
  github_token: $GITHUB_TOKEN
  on:
    branch: master